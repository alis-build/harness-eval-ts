adapter: claude-code
defaultConfig:
  model: claude-sonnet-4-6
  timeoutMs: 60000
  claudeCode:
    permissionMode: bypassPermissions

matrix:
  - label: default
    config:
      claudeCode:
        pluginDirs: [./plugins/v1.0.0]
